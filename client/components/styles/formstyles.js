import styled from 'styled-components';

const Form = styled.form`
  display: flex;
  flex-direction: column;
`;

const FormItemSpace = styled.div`
  padding: 8px 0;
`;

const Title = styled.h1`
  margin: 16px 0;
  font-size: 38px;
`;

export { Form, Title, FormItemSpace };
